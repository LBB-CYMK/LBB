# 方案设计

## 一、功能需求分析

### 1. 功能性需求

系统面向**运维人员**，围绕「查看设备磨损 → 获得预测与剩余寿命 → 据此安排检修」的业务闭环设计。

（1）**数据处理功能**：支持设备磨损时序数据导入、异常值剔除、数据归一化、数据集划分。
　交互流程：执行 `python src/generate_data.py` 生成原始数据 → 执行 `python src/preprocess.py` 完成清洗与样本构建 → 输出清洗后数据、时序样本与统计报告。

（2）**模型预测功能**：基于 RNN 网络完成磨损量预测、部件剩余寿命 RUL 计算。
　交互流程：输入设备最近 20 个周期的 `[磨损量, 温度]` → 模型单步前向预测下一周期磨损量 → 以滚动前向模拟估算剩余寿命 RUL。

（3）**故障预警功能**：根据预测磨损量与 RUL 自动判断部件状态，输出**三级预警 + 可执行检修建议**。
　交互流程：预测完成后自动判定 →
　- **正常** → 建议「按计划周期检修」
　- **预警**（RUL ≤ 30 周期）→ 建议「提前准备备件，安排近期停机检查」
　- **严重预警**（RUL ≤ 15 周期，或预测磨损量已越过失效阈值）→ 建议「立即安排停机检修，避免非计划停机」

（4）**数据可视化功能**：展示磨损变化曲线、失效阈值线、预测结果、设备状态与预警记录。

（5）**记录管理功能**：预测记录与预警事件写入数据库，支持历史查看与追溯。

### 2. 非功能性需求

（1）**实时性**：模型推理为单次前向传播，毫秒级返回，满足小型系统实时预测需求。
（2）**稳定性**：固定随机种子保证数据生成与训练可复现；数据库初始化幂等。
（3）**易用性**：结构清晰、模块化分层（数据/模型/预测/数据库/后端/前端），便于后期迭代优化。
（4）**可测试性**：提供 pytest 自动化测试，覆盖模型、预测、数据库、API 四层。

## 二、方案论证

### 1. 与传统维护方式的对比

传统设备维护多采用**定期检修 + 固定阈值判断**，存在检修浪费、故障滞后等问题。
本方案采用**深度学习时序预测模型**，根据历史磨损数据拟合退化趋势，实现提前预判：

1. 不依赖固定阈值，可自适应不同设备的退化速率差异；
2. 利用 RNN 记忆时序特征，从磨损与温度的联合演化中预测趋势；
3. 由模型外推剩余寿命，把「事后维修/定期检修」变为「按需检修」；
4. 结构轻量化，CPU 可训练、零配置可部署，适合课程设计落地。

### 2. 关键技术选型论证

> 以下每个维度给出**候选方案对比、选择理由与代价局限**，而非只给结论。

#### 选型一：退化数据来源 —— 自建仿真 vs 公开数据集 vs 企业数据

| 方案 | 优点 | 缺点 |
|---|---|---|
| A. NASA C-MAPSS 真实数据 | 真实、公认的 RUL 基准数据集 | 2.6 万条×4 子集，含 21 路传感器与 3 种工况，**需大量特征工程**；无「磨损量」直接标签，RUL 需自行标定；课程周期内难以吃透 |
| B. 自建指数退化数据 | 退化规律明确、**有磨损量真值**、完全可控可复现；可定向注入异常值验证清洗流程 | 属仿真数据，噪声模型比真实工况简单 |
| C. 企业真实数据 | 最贴合实际 | 课程不要求且无法获取 |

**选择 B**：本课程重点考核「预测性维护的方法链路是否走通」而非数据规模。
自建数据能精确控制退化规律与噪声，**便于验证每一步算法是否真的起作用**（如异常值剔除率可逐项核对）。
同时保留 A 作为公开数据对照来源，说明方法的可迁移性。

> **诚实边界**：本项目训练与测试**全部使用自建仿真数据**，NASA C-MAPSS 仅作为公开数据来源提及，**未实际用于训练或验证**。

#### 选型二：预测模型 —— RNN vs LSTM vs GRU vs Transformer

| 模型 | 长程依赖 | 参数量/训练成本 | 本任务适配性 |
|---|---|---|---|
| **RNN** | 弱（梯度易衰减） | **最小** | 窗口仅 **20 步**，序列短，衰减问题不突出 |
| LSTM | 强 | 约 4×（三个门） | 能力强但对本任务**冗余** |
| GRU | 较强 | 约 3× | 折中方案 |
| Transformer | 最强 | 最大，小数据易过拟合 | 20 步短序列上**注意力优势无法体现** |

**选择 RNN**：① 序列长度只有 20 步，RNN 的梯度衰减在此长度不构成瓶颈；
② 数据量约 1.2 万条，参数量小的模型更不易过拟合；
③ 与课程「循环神经网络时序预测（RNN/ReLU）」专题直接对应；
④ CPU 数十秒可完成训练，符合「无需 GPU」的资源要求。

**代价与局限**：若窗口加长到 100 步以上，RNN 会明显弱于 LSTM/GRU —— 这是后续改进方向。

#### 选型三：异常值检测 —— 3σ vs 全局 IQR vs 去趋势 + IQR

| 方案 | 原理 | 在单调退化序列上的表现 |
|---|---|---|
| A. 3σ 准则 | 均值 ± 3 倍标准差 | 均值被趋势主导，尾部残差大，误判严重 |
| B. 全局 IQR | 对原始值求四分位距 | 单调上升序列的**尾部整段被判为异常**（实测误标约 19%） |
| C. **去趋势 + IQR** | 先扣局部趋势得近似同方差残差，再对残差做 IQR | 残差同方差，误判率降至方法固有水平（约 1%） |

**选择 C**：磨损量单调上升，直接做全局 IQR 会把趋势尾部误判为异常。
关键实现细节是去趋势采用「**两侧邻域中位数**」而非滑动中位数 ——
对单调序列，含当前点的滑动中位数恒等于当前点、残差为 0；
取「除当前点外」的两侧邻域中位数才能既估计趋势、又排除野值干扰。

**效果**：剔除 405 个异常值（磨损 322、温度 281），与约 1.5% 的注入比例吻合。

#### 选型四：RUL 估算方法 —— 直接回归 vs 滚动前向模拟

| 方案 | 做法 | 优点 | 缺点 |
|---|---|---|---|
| A. 直接回归 | 另训一个模型直接输出 RUL | 单次前向，快 | 需依赖 RUL 标签，而该标签本身是按分段线性**假设**算出的，质量受假设限制；且与磨损预测成为**两套独立模型** |
| B. **滚动前向模拟** | 复用**同一个**磨损预测模型，反复预测→回填窗口，直到越过失效阈值 | **不额外训练模型**；与磨损预测共享同一套权重，逻辑自洽；天然对应「状态空间搜索」专题 | 需多次前向（20~125 次），但单步仅毫秒级 |

**选择 B**：避免引入第二套模型和不可靠的 RUL 标签，
让「磨损预测」与「寿命估算」共享同一物理依据（都是模型对磨损演化的外推）。

#### 选型五：系统架构与技术栈

| 维度 | 候选 | 选择 | 理由 |
|---|---|---|---|
| 架构 | C/S vs **B/S** | **B/S** | 任务书二者皆可；B/S **零安装**，浏览器直接演示，答辩现场最稳妥 |
| 后端 | Flask vs **FastAPI** | **FastAPI** | ①自动生成交互式 API 文档，演示友好；②Pydantic 请求校验，减少手写校验；③原生 async，契合「实时预测」需求 |
| 数据库 | MySQL vs **SQLite** | **SQLite** | 单机演示无需独立数据库服务；**零配置、随项目走**，一键启动即可运行；标准库自带，降低环境依赖 |
| 前端 | React vs **原生 HTML + ECharts** | **原生 HTML + ECharts** | 单页展示需求，引入 Node/npm 构建链会增加部署复杂度且与考核点无关；ECharts CDN 即满足曲线可视化 |
| 可视化 | 仅 Matplotlib vs **两者兼用** | **Matplotlib + ECharts** | Matplotlib 输出静态分析图（训练曲线、预处理对比）供报告使用；ECharts 负责前端交互式展示 |

**代价与局限**：SQLite 不适合多用户并发写入；若扩展为车间多终端系统需迁移至 MySQL。

## 三、方案规划

### 1. 整体技术栈
- 编程语言：Python
- 数据处理：Numpy、Pandas
- 深度学习框架：PyTorch
- 模型算法：RNN 循环神经网络、ReLU 激活函数
- 后端服务：FastAPI + uvicorn
- 数据库：SQLite
- 可视化：Matplotlib（服务端）+ ECharts（前端）
- 版本管理：Git + GitHub

### 2. 外部资源
无需服务器、无需 GPU，普通电脑 CPU 即可训练运行；实测训练耗时数十秒，资源轻量化。

### 3. 数据集来源
采用**自建指数退化数据集**（`w(t) = w0 + A·(exp(B·t) − 1) + ε`），30 台设备共 12000 条样本，
退化规律源自 RUL 预测领域的经典指数退化模型，直接提交至仓库 `/data` 目录，无需外部下载。

公开数据集**仅作对照来源参考**：NASA C-MAPSS 涡扇发动机退化数据集
（<https://github.com/edwardzjl/CMAPSSData>）—— 本项目未使用其训练或验证。

### 4. 后端算法方案
整体流程：原始数据 → 异常值检测 → 归一化处理 → 时序样本构建 → RNN 模型训练（含超参数搜索）
→ 磨损预测 & RUL 计算 → 故障预警输出（含检修建议）

### 5. 前端展示方案
采用 B/S 架构，浏览器单页前端（HTML + ECharts）+ 后端 REST API，走通完整业务流程：
- 设备选择与档案展示（失效周期、退化参数）
- 磨损趋势曲线（含失效阈值线）
- 模型预测结果（下一周期磨损量、剩余寿命 RUL）
- 故障预警状态（正常/预警/严重预警）与**检修建议**
- 预警事件与预测记录查询

## 四、技术路线
1. 数据获取与数据清洗（去趋势 + IQR 异常值检测、Min-Max 归一化）
2. 构建时序数据集、按设备划分训练集与测试集（防数据泄漏）
3. 搭建 RNN 神经网络模型，设置 ReLU 激活与训练迭代机制
4. 超参数网格搜索、模型训练、误差分析
5. 实现磨损预测、RUL 估算与三级预警
6. 前后端集成、可视化结果展示、自动化测试
7. 项目整理、文档完善、代码上传仓库

## 五、项目进度计划
1. 本周：完成选题说明、方案设计文档编写与仓库推送
2. 第1阶段：完成数据预处理、数据集构建
3. 第2阶段：搭建 RNN 模型、完成模型训练
4. 第3阶段：实现预测功能、可视化功能开发
5. 第4阶段：系统调试、整体整合、最终答辩材料整理

## 六、参考文献
[1] 人工智能基础与深度学习应用教程
[2] 基于循环神经网络的设备剩余寿命预测研究
[3] 智能制造设备数据预处理与故障诊断技术研究
[4] Python数据分析与机器学习实战
[5] 时序数据深度学习预测方法综述
