# 需求分析发现（findings）

## 权威要求来源
`制造智能技术课程设计任务书(1).pdf`（3 页）为课程硬性要求；其余 MD 为项目自拟文档。

## 任务书硬性要求（已核对）
1. **选题三要素**：制造/工业场景 ✅；≥3 章技术手段 ⚠️；完整 B/S 或 C/S 可运行 demo ❌（当前缺）。
2. **架构四件套**：前端 UI（走通完整业务流程）+ 后端服务(API/服务层) + 数据库 + 算法模块(≥3 技术方向)。
3. **课程内容 6 项**：vibe coding 学习、git、选题提交、数据整理、系统开发与集成调试(含自动化测试)、成果提交(报告+3 分钟视频+答辩 PPT)。
4. **技术方向覆盖**：答辩须说明每个方向实际作用，**装饰性引入不计入覆盖**。
5. **报告章节**：背景调研、方案设计、数据资源构建、系统实现(前端/后端/数据库/各算法模块)、测试与集成调试、AI 使用披露、总结展望。
6. **提交物清单**：①代码仓库(git+历史+运行脚本+readme) ②需求规格说明书 ③设计报告 ④3 分钟演示视频 ⑤过程档案(prompt 日志、打卡记录)。
7. **注意事项**：如实披露 AI 工具与 prompt 策略；不能照搬 AI 内容、答辩要讲清 AI 生成代码；项目须通过自带自动化测试。
8. **评分**：过程 30% + 成果 50%(设计说明书 25% + 程序 25%) + 答辩 20%。

## 关键差距（按失分严重度）
1. **架构不完整**（最高）：现只有 2 个脚本，缺前端 UI、后端 API、数据库。→ 阶段 4 补齐。
2. **「智能搜索」落地风险**（高）：选题说明称用启发式搜索调参，需真正实现，否则答辩判为装饰性。→ 训练加超参网格搜索。
3. **缺需求规格说明书**（高）→ 阶段 4 补。
4. **无自动化测试**（高）→ 阶段 4 补 pytest。
5. **RNN 模型、预测、RUL、预警逻辑未实现**（中）→ 阶段 2–3。
6. **答辩材料（视频/PPT）**（中）→ 阶段 4 末。

## 技术栈决策（已确认环境）
- Python 3.9.13；torch 2.8.0+cpu；fastapi 0.128.8；uvicorn 0.39.0；numpy/pandas/matplotlib 可用；pytest 需 `pip install pytest`。
- 选定：PyTorch RNN + FastAPI + SQLite(标准库 sqlite3) + 单页前端(HTML+ECharts CDN) + pytest。
