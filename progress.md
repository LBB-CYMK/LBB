# 进度记录

## 阶段 1（已完成 ✅）
数据来源准备（自建指数磨损数据 30 设备/1.2 万条）、数据预处理（去趋势+IQR+归一化+RUL+滑动窗口）、提示词追溯。

## 阶段 2（已完成 ✅）
- `src/model.py`：`WearRNN` 模型（RNN+ReLU）
- `src/train.py`：超参数网格搜索（智能搜索）+ 训练 + 保存模型
- 训练结果：最优 hidden=32/lr=1e-3，独立测试集 MAE≈0.0041、RMSE≈0.0052
- 产物：`models/wear_rnn.pt`、`outputs/train_loss.png`、`outputs/train_report.json`

## 阶段 3（已完成 ✅）
- `src/predict.py`：单步磨损预测 + 滚动前向 RUL 估算（状态空间搜索）+ 三级故障预警 + 可视化
- 产物：`outputs/demo_prediction.png`

## 阶段 4（已完成 ✅）
- `src/database.py`：SQLite 数据层（devices/predictions/alerts 三表）
- `src/app.py`：FastAPI 后端（REST API + 静态前端 + lifespan 初始化）
- `web/index.html`：单页前端 UI（选设备→看曲线→预测→RUL/预警）
- `tests/`：pytest 自动化测试（模型/预测/数据库/API 共 14 用例，全部通过）
- `需求规格说明书.md`：补齐提交物
- 更新 README / 方案设计 / prompt 追溯
- `start.bat`：一键启动（同目录搜索解释器，不依赖 PATH；端口 8001）

---

## 评审意见整改：选题与选型具体化（已完成 ✅）

**评审意见**：「选型和选题未具体」。

**整改思路**：把「我用了 X」升级为「在 A/B/C 中为什么选 X，代价是什么，验证数据是多少」；
把宽泛的「设备」收敛到具体对象/信号/判据/闭环。

### 具体化后的场景（统一口径，各文档一致）
- **对象**：旋转机械磨损型退化部件，以数控机床**主轴轴承**为参照；失效模式＝渐变型磨损失效
- **信号**：磨损量 w(t)（mm，直接指标）+ 温度 T(t)（℃，间接信号）
- **判据**：磨损量达 **1.0 mm**；30 台设备寿命 244~597 周期
- **闭环**：运维看板 → RUL + 三级预警 → 检修决策（计划检修 / 提前备件 / 立即停机）

### 5 个选型维度（均含候选对比、理由、代价局限）
| 维度 | 选择 | 核心理由 |
|---|---|---|
| 数据来源 | 自建指数退化数据 | 有磨损量真值、可控可复现 |
| 预测模型 | RNN + ReLU | 窗口仅 20 步；1.2 万条小数据宜小模型 |
| 异常值检测 | 去趋势 + IQR | 全局 IQR 误标约 19% |
| RUL 估算 | 滚动前向模拟 | 复用同一套权重，不依赖假设标签 |
| 架构技术栈 | B/S + FastAPI + SQLite | 零安装易演示、零配置 |

### 改动文件
| 文件 | 改动 |
|---|---|
| `选题说明.md` | 全面重写：场景四要素表 + 可验收指标表 + 4 个技术方向的**实际作用**与落地代码位置 |
| `方案设计.md` | 功能需求补交互流程；方案论证新增 **5 个选型维度对比**；修正数据集来源表述（诚实说明自建仿真，C-MAPSS 未用于训练） |
| `README.md` | 项目简介新增「应用场景」与「关键技术选型」两张表 |
| `需求规格说明书.md` | 背景具体化；新增 **2.2 业务闭环**；FR-6 补充检修建议 |
| `答辩PPT大纲.md` | 新增 **P4 选型论证页**；P2 场景具体化；问答新增选型追问应对 |
| `src/predict.py` | 新增 `MAINTENANCE_ADVICE` + `get_maintenance_advice`；`check_alert` 输出「判定依据 + 检修建议」 |
| `src/app.py` | 预测接口新增 `advice` 字段 |
| `web/index.html` | 醒目展示「检修建议」，下方给出判定依据 |

### 业务闭环的代码落地（不只停留在文档）
预警不再只输出状态，而是输出**可执行检修建议**：
- normal → 建议按计划周期检修
- warning → 建议提前准备备件，安排近期停机检查
- critical → 建议立即安排停机检修，避免非计划停机

## 验证
- `python -m pytest` → **14 passed**（代码改动后复跑仍全绿）
- `check_alert` 三档实测：normal / warning(RUL=20) / critical(RUL=5 与超阈值) 输出均正确
- API 冒烟：`POST /api/predict` 返回 200，含 `advice` 字段
- `uvicorn src.app:app` 冒烟：health / predict / 前端页面均正常返回

## 备注：工作流执行失败
原计划用 17 个智能体的工作流做「并行审计 → 统一规范 → 改写 → 对抗验证」，
但 **5 个智能体全部因网络错误中断**（`Connection closed mid-response`、`UNKNOWN_CERTIFICATE_VERIFICATION_ERROR`），
journal 中零 result。改用**内联方式**完成全部具体化工作（单条流更稳）。
待网络恢复后，可补充一次独立的对抗性事实核对。

---

## 设计说明书撰写（已完成 ✅）

按任务书要求撰写 `设计说明书.md`，共 8 章，约 9000 中文字：

| 章节 | 内容 |
|---|---|
| 第一章 | 背景调研与研究意义（含数据来源调研） |
| 第二章 | 方案设计（含 5 项选型论证 + 总体架构） |
| 第三章 | 数据集构建（生成原理、异常值注入、预处理五步、最终规模） |
| 第四章 | 技术方向详细设计（预处理 / RNN / 智能搜索 / 版本控制） |
| 第五章 | 系统实现与界面设计（后端、数据库、前端、代码结构） |
| 第六章 | 系统测试与调试（14 用例 + 7 个真实调试问题） |
| 第七章 | AI 工具使用披露（工具、提示词策略、4 个出错案例） |
| 第八章 | 总结与展望（完成情况、设计取舍、局限与改进方向） |

### 撰写过程中的质量把控

1. **超参数搜索表数据修正**：初稿中第 3、4 组配置的 val_mse 是按控制台 6 位小数输出推算的
   （写成 2.51e-5、2.53e-5），经核对实际为 **2.4590e-5、2.4700e-5**。
   为避免这类「看似精确却经不起核对」的数字，修改 `src/train.py` 使其保存**每一组**候选配置的
   验证集误差（`search_results` 字段），重跑训练后取精确值填入。

2. **修正过度解读**：初稿把 hidden_size 64 变差归因为"过拟合"。
   核对后发现四组误差极差不到 2.2%，且网格**并非全因子设计**（缺 (16,3e-3)、(64,1e-3)），
   无法把差异单独归因于某个超参数。已改为如实说明"模型在此参数范围内不敏感"，
   并指出该网格的实际价值是排除明显更差的配置而非找到显著更优点。

3. **全文事实核对**：逐一比对报告数字与代码常量/产物——
   数据生成参数、预处理参数、训练参数、模型结构、预警阈值、路由数（7）、
   测试用例数（14）、指标换算（RMSE 0.0051mm / MAE 0.004mm / 占阈值 0.40%）全部一致。

4. **重跑训练的可复现性**：seed=42 下重跑，测试集指标与模型权重**完全一致**，证明结果可复现。

### 重要提醒（已在交付时向用户说明）
任务书要求「不要将 AI 生成内容直接作为报告内容」，且答辩时会随机指定代码段现场讲解。
本说明书为基于真实代码与实测数据的初稿，**使用者须逐节读懂并用自身语言改写**后方可作为最终提交。
