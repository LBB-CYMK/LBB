# 需求规格说明书

> 项目：基于深度学习的设备磨损趋势预测与故障预警系统
> 版本：1.0　·　日期：2026-08-31

## 一、引言

### 1.1 编写目的
本文档定义「基于深度学习的设备磨损趋势预测与故障预警系统」的功能与非功能需求，作为系统设计、开发与验收的依据，对应课程设计提交物清单中的「需求规格说明书」。

### 1.2 项目背景
旋转机械中的**磨损型退化部件**（以数控机床**主轴轴承**为参照对象）长期运行会产生磨损，磨损量达到 **1.0 mm** 即判定失效。
传统「定期检修 + 固定阈值」方式存在检修浪费、故障滞后等弊端。

本项目按运行周期采集两路信号：**磨损量 w(t)**（mm，直接退化指标）与**温度 T(t)**（℃，与磨损强相关的间接信号），
运用深度学习时序模型（RNN）对磨损量进行趋势预测，估算剩余使用寿命（RUL），
并在磨损接近失效前自动输出三级故障预警与**可执行检修建议**，实现部件智能监测。

> 说明：本项目为课程设计，采用自建仿真数据建模，定位为「以该类部件为参照对象的建模与决策链路验证」，非真实产线部署。

### 1.3 术语定义
| 术语 | 说明 |
|---|---|
| RUL | 剩余使用寿命（Remaining Useful Life），距失效的剩余运行周期数 |
| 滑动窗口 | 取连续 N 个周期的特征作为模型输入的时间片段（本项目 N=20） |
| 归一化 | 将特征线性映射到 [0,1]，消除量纲差异 |
| 失效阈值 | 磨损量达到 1.0mm 判定设备失效 |

## 二、总体描述

### 2.1 产品概述
系统面向**设备运维人员**，采用 B/S 架构：前端提供浏览器交互界面，后端提供 REST API 承载业务逻辑，
SQLite 数据库存储设备档案与预测/预警记录，算法模块完成数据预处理、RNN 预测与 RUL 估算。

### 2.2 业务闭环
系统不是单纯的「预测演示」，而是落到运维决策上的闭环：

```
运维人员选择设备 → 查看磨损趋势曲线（含失效阈值线）
        → 触发预测 → 获得「下一周期磨损量 + 剩余寿命 RUL + 预警等级」
        → 系统给出可执行检修建议 → 据此安排检修计划
        → 预测与预警记录入库，支持事后追溯
```

各级预警对应的运维决策：

| 等级 | 触发条件 | 检修建议（系统输出） |
|---|---|---|
| normal | RUL > 30 周期 | 按计划周期检修 |
| warning | 15 < RUL ≤ 30 周期 | 提前准备备件，安排近期停机检查 |
| critical | RUL ≤ 15 周期，或预测磨损量已越过失效阈值 | 立即安排停机检修，避免非计划停机 |

### 2.2 系统架构
```
浏览器前端(HTML+ECharts)  ──HTTP──>  FastAPI 后端(API 层)
                                          │ 调用
                                    算法服务层(predict.py)
                                          │ 读写
                                     SQLite 数据库(database.py)
```

### 2.3 运行环境
- 操作系统：Windows / Linux（普通电脑 CPU 即可）
- 运行环境：Python 3.8+
- 依赖：numpy、pandas、matplotlib、torch、fastapi、uvicorn、pytest

## 三、功能需求

| 编号 | 功能 | 描述 |
|---|---|---|
| FR-1 | 数据生成 | 基于指数退化模型生成 30 台设备磨损/温度时序数据，注入约 1.5% 异常值 |
| FR-2 | 数据预处理 | 邻域中位数去趋势 + IQR 异常值剔除、Min-Max 归一化、RUL 标签计算、滑动窗口样本构建、按设备划分数据集 |
| FR-3 | 模型训练 | 搭建 RNN 模型，超参数网格搜索（智能搜索）后训练，输出训练曲线与指标 |
| FR-4 | 磨损预测 | 输入设备近 20 周期特征，预测下一周期磨损量 |
| FR-5 | RUL 估算 | 滚动前向模拟（状态空间搜索）估算设备剩余寿命 |
| FR-6 | 故障预警 | 基于预测磨损量与 RUL 判定三级状态并输出**可执行检修建议**（正常→按计划检修；预警→提前备件、安排近期停机检查；严重预警→立即停机检修） |
| FR-7 | 设备查询 | 查询设备档案（失效周期、退化参数）与磨损/温度历史曲线 |
| FR-8 | 记录管理 | 预测记录与预警事件写入数据库并可查询 |
| FR-9 | 可视化 | 磨损趋势曲线、预测曲线、RUL 对比、预警状态展示 |

## 四、非功能需求

| 编号 | 类别 | 描述 |
|---|---|---|
| NFR-1 | 实时性 | 模型推理为单次前向传播，毫秒级返回，满足小型系统实时预测 |
| NFR-2 | 稳定性 | 固定随机种子保证数据与训练可复现；数据库幂等初始化 |
| NFR-3 | 易用性 | 模块化分层（数据/模型/预测/数据库/后端/前端），便于迭代 |
| NFR-4 | 可测试性 | 提供 pytest 自动化测试，覆盖模型、预测、数据库、API |

## 五、接口需求（REST API）

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | / | 前端页面 |
| GET | /api/health | 服务健康检查 |
| GET | /api/devices | 设备列表 |
| GET | /api/devices/{unit_id}/history | 设备磨损/温度历史 |
| POST | /api/predict | 预测 + RUL + 预警（入参 `{"unit_id": int}`） |
| GET | /api/predictions | 最近预测记录 |
| GET | /api/alerts | 最近预警事件 |

## 六、数据需求

### 6.1 数据库表结构（SQLite）

**devices（设备档案）**
| 字段 | 类型 | 说明 |
|---|---|---|
| unit_id | INTEGER PK | 设备编号 |
| failure_cycle | INTEGER | 失效周期 |
| w0 / A / B | REAL | 指数退化参数 |
| created_at | TEXT | 入库时间 |

**predictions（预测记录）**
| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER PK | 自增主键 |
| unit_id | INTEGER | 设备编号 |
| pred_wear_mm | REAL | 预测磨损量（mm） |
| rul | INTEGER | 估算剩余寿命 |
| alert_level | TEXT | normal / warning / critical |
| message | TEXT | 提示信息 |
| created_at | TEXT | 预测时间 |

**alerts（预警事件）**：仅记录非正常状态，字段同 predictions 的预警相关部分。

### 6.2 数据文件
- `data/raw/wear_raw.csv`、`data/raw/units_summary.csv`：原始数据
- `data/processed/wear_processed.csv`、`sequences.npz`：预处理产物
- `models/wear_rnn.pt`：训练好的模型权重

## 七、验收标准
1. 运行 `python src/train.py` 可训练出模型并输出指标（测试集 MAE < 0.01）。
2. 运行 `python -m pytest` 全部用例通过。
3. 运行 `uvicorn src.app:app` 后浏览器可完成「选设备 → 看曲线 → 预测 → 查看 RUL/预警」完整流程。
