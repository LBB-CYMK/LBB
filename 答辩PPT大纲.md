# 答辩 PPT 大纲

> 题目：面向旋转机械磨损型部件的退化趋势预测与预测性维护决策系统
> ——基于 RNN 时序建模的磨损量预测、剩余寿命(RUL)估算与三级故障预警
>
> 课程：制造智能技术课程设计
> 要求：自述 3 分钟 + 问答 7 分钟；教师随机指定 1–2 处 AI 生成代码段现场讲解
>
> **本版针对评审意见「选题和选型未具体」做了专项强化**：P2 场景具体化、**P4 选型论证（新增）**、
> 附录问答补充选型追问应对。

## 一、总原则

1. **3 分钟约 14 页**，平均每页 12–15 秒，重点页（场景/选型/架构/智能搜索）多给时间。
2. **每条技术方向必须讲清「在系统里的实际作用」**——装饰性引入会被判不计入覆盖。
3. **选题要落到具体对象、信号、判据、闭环**；**选型要给出候选对比与理由**，不能只报结论。
4. **AI 使用披露是必答题**，单独一页，主动讲、讲具体。
5. 全程围绕一句话主线：**「用 RNN 替代固定阈值，预测磨损 → 估算剩余寿命 → 输出可执行的检修决策」**。

---

## 二、逐页大纲

### P1 封面（5 秒）
- 题目：面向旋转机械磨损型部件的退化趋势预测与预测性维护决策系统
- 副标题：基于 RNN 的磨损预测 · RUL 估算 · 三级预警
- 姓名/学号、课程名

### P2 项目背景与具体场景（15 秒）★应对「选题未具体」
- 痛点：传统**定期检修 + 固定阈值** → 检修浪费、故障滞后
- **场景四要素**（一页表格讲清）：
  | 要素 | 内容 |
  |---|---|
  | 对象 | 旋转机械磨损型退化部件，以数控机床**主轴轴承**为参照；失效模式＝**渐变型磨损失效** |
  | 信号 | **磨损量 w(t)**（mm，直接退化指标）+ **温度 T(t)**（℃，与磨损强相关的间接信号） |
  | 判据 | 磨损量达 **1.0 mm** 判定失效；30 台设备寿命 244 ~ 597 周期 |
  | 闭环 | 运维看板 → RUL + 三级预警 → 检修决策（计划检修 / 提前备件 / 立即停机） |
- 价值：把「定期检修」升级为「按需检修」，减少非计划停机

### P3 需求与目标（10 秒）
- 功能：数据清洗 → 磨损预测 → RUL 估算 → 三级预警（含检修建议）→ 可视化
- 目标：交付**完整 B/S 可运行 demo** + 通过自动化测试
- 量化指标：预测 MAE ≈ **0.004 mm**（约为失效阈值 1.0 mm 的 **0.4%**）

### P4 选型论证（20 秒）★新增，应对「选型未具体」
候选 → 选择 → 理由，一页表格讲完：

| 维度 | 候选 | 选择 | 一句话理由 |
|---|---|---|---|
| 数据来源 | C-MAPSS / **自建仿真** / 企业数据 | 自建指数退化数据 | 有磨损量真值、可控可复现，便于逐项验证每步算法 |
| 预测模型 | **RNN** / LSTM / GRU / Transformer | RNN + ReLU | 窗口仅 20 步，衰减不构成瓶颈；1.2 万条小数据宜小模型 |
| 异常检测 | 3σ / 全局 IQR / **去趋势+IQR** | 去趋势 + IQR | 单调趋势下全局 IQR 误标约 19%；去趋势后残差同方差 |
| RUL 估算 | 直接回归 / **滚动前向模拟** | 滚动前向模拟 | 复用同一套权重，不依赖假设得到的 RUL 标签 |
| 架构栈 | C/S vs **B/S**；Flask vs **FastAPI**；MySQL vs **SQLite** | B/S + FastAPI + SQLite | 零安装易演示、自带 API 文档、零配置随项目走 |

> 主动说明代价：仿真数据噪声模型比真实工况简单；窗口加长到 100+ 步时 RNN 会弱于 LSTM/GRU。

### P5 系统总体架构（20 秒）★重点
- 架构图：
  ```
  浏览器前端(HTML+ECharts) → FastAPI 后端(API) → 算法服务层 → RNN 模型
                                    ↓ 读写
                             SQLite 数据库
  ```
- 强调：**完整 B/S 四件套齐全**——前端 UI / 后端服务 / 数据库 / 算法模块
- 技术栈：Python + PyTorch + FastAPI + SQLite + ECharts

### P6 数据资源构建（15 秒）
- 自建**指数退化模型**数据：`w(t) = w0 + A·(e^{Bt}−1) + ε`，30 台设备约 1.2 万条
- 注入约 1.5% 传感器野值用于验证异常检测
- 预处理流水线：去趋势+IQR 剔除 → Min-Max 归一化 → RUL 标签 → 滑动窗口 → 按设备划分
- 结果：训练集 `(9057,20,2)` / 测试集 `(2343,20,2)`（**按设备划分防泄漏**）

### P7 技术方向① 数据预处理与异常值识别（12 秒）
- **实际作用**：保证训练数据干净
- 关键点：单调趋势直接做 IQR 会把趋势尾部误判 → 先「两侧邻域中位数去趋势」再对残差 IQR
- 效果：共剔除 **405 个异常值**（磨损 322 + 温度 281）

### P8 技术方向② RNN 时序预测（15 秒）★重点
- 模型 `WearRNN`：RNN(ReLU) → 取最后时间步隐藏态 → 全连接回归头
- 输入前 20 周期 `[磨损, 温度]` → 输出下一周期磨损量
- 指标：独立测试集 **MAE≈0.004 mm、RMSE≈0.0051 mm**
- 配图：训练损失曲线 + 预测 vs 真实对比图

### P9 技术方向③ 智能搜索与状态空间搜索（13 秒）★易被质疑
- **两处真实落地**（非装饰）：
  1. **超参数网格搜索**：对 `hidden_size × lr` 枚举 4 组，以验证集 MSE 择优 → 最优 `hidden=32, lr=1e-3`，**直接决定最终模型**
  2. **滚动前向模拟估算 RUL**：反复「预测下一周期 → 回填窗口 → 再预测」，在状态空间中搜索磨损越过失效阈值的时刻
- 强调：二者分别参与了**训练调参**与**寿命预测**

### P10 技术方向④ 版本控制与工程部署（8 秒）
- Git 全程小步提交 + 有意义提交说明，托管 GitHub
- `prompt/` 保留 AI 会话日志；README + 一键启动脚本 `start.bat`

### P11 系统实现与测试（20 秒）
- 后端：FastAPI 提供 7 个 REST 接口（设备查询/历史/预测/预警/记录）
- 数据库：SQLite 三表（`devices` / `predictions` / `alerts`）
- 前端：单页 UI，走通「**选设备 → 看曲线 → 预测 → 查看 RUL/预警/检修建议**」
- **业务闭环落地**：预警输出不止状态，还给出**可执行检修建议**（正常→计划检修 / 预警→提前备件 / 严重→立即停机）
- 测试：pytest **14 用例全部通过**（模型/预测/数据库/API 四层）

### P12 AI 使用披露（15 秒）★必答
- 工具：Claude Code (CLI)，模型 deepseek-v4-pro
- 关键 prompt 策略：任务拆解、约束说明、验收标准、先读代码再改
- **AI 出错案例及纠正**（体现人工复核）：
  1. IQR 对单调趋势误判约 19% → 改为「去趋势 + IQR」
  2. pip 与 python 指向不同环境 → 改用 `python -m pip`
  3. FastAPI `on_event` 已弃用 → 改用 lifespan 生命周期
- 承诺：所有 AI 输出均经本人理解、修改、测试，非照搬

### P13 总结与展望（10 秒）
- 成果：完整可运行 B/S demo，覆盖 4 个技术方向，业务闭环落到检修决策
- 展望：换 LSTM/GRU 应对更长窗口；接入 NASA C-MAPSS 验证泛化性；自动探测空闲端口

### P14 致谢 / 问答

---

## 三、3 分钟时间分配

| 环节 | 页 | 时长 |
|---|---|---|
| 封面 + 背景场景 + 需求 | P1–P3 | 30 秒 |
| **选型论证** | P4 | 20 秒 |
| 架构 | P5 | 20 秒 |
| 数据 | P6 | 15 秒 |
| 4 个技术方向 | P7–P10 | 45 秒 |
| 系统实现与测试 | P11 | 20 秒 |
| AI 披露 + 总结 | P12–P13 | 25 秒 |
| 缓冲 | — | 5 秒 |
| **合计** | | **180 秒** |

---

## 四、问答环节准备（7 分钟）

教师会**随机指定 1–2 处 AI 生成代码段**，需现场讲清作用与逻辑。

### 4.1 代码段讲解准备

| 代码段 | 讲解要点（一句话） |
|---|---|
| `src/model.py` 的 `WearRNN.forward` | RNN 逐时间步编码 20 周期输入，取最后一个时间步的隐藏状态作为整段序列的摘要，再经两层全连接输出下一周期磨损量 |
| `src/preprocess.py` 的 `_neighbor_median_trend` | 用「除当前点外」两侧邻域的中位数估计局部趋势，`np.delete(arr, len(arr)//2)` 就是为了排除当前点，避免野值污染趋势估计 |
| `src/predict.py` 的 `roll_forward_rul` | 状态空间前向模拟：反复预测下一步磨损并回填窗口，搜索首次越过失效阈值的周期数，即剩余寿命 |
| `src/predict.py` 的 `check_alert` / `MAINTENANCE_ADVICE` | 判定依据 + 检修建议拼接，把预警等级映射为运维可执行决策，形成业务闭环 |
| `src/train.py` 的超参网格搜索 | 对 hidden_size×lr 构成的状态空间枚举，以验证集 MSE 为评价函数择优，对应「智能搜索」专题 |
| `src/database.py` 的表结构 | 三张表：设备档案、预测记录、预警事件，异常状态才写 alerts，实现「预测留痕 + 预警归档」 |
| `src/app.py` 的 `lifespan` | 应用启动时建表并播种设备档案，替代已弃用的 on_event，保证测试与运行时数据库就绪 |

**应对技巧**：被指定到没把握的段落，按「**这行代码做什么 → 数据从哪里来到哪里去 → 为什么这样写**」三句话展开，讲清逻辑而非背诵语法。

### 4.2 选型与选题追问应对 ★应对「选型未具体」

| 可能的问题 | 回答要点 |
|---|---|
| 「为什么不用 LSTM？」 | 窗口只有 20 步，RNN 的梯度衰减在此长度不构成瓶颈；数据量 1.2 万条，小模型更不易过拟合；且课程专题就是 RNN/ReLU。窗口加长到 100+ 步时会考虑 LSTM |
| 「自建数据不算真实数据吧？」 | 是仿真数据，但**退化规律（指数退化模型）是 RUL 领域的经典模型**；本项目重点验证方法链路，且保留了 NASA C-MAPSS 作为公开数据对照来源（**未实际用于训练**） |
| 「RUL 标签是自己假设的，可靠吗？」 | 正因如此，**没有用假设标签去训第二个模型**，而是用滚动前向模拟从磨损预测模型直接外推 RUL，避免依赖不可靠标签 |
| 「做了具体是什么设备？」 | 以**数控机床主轴轴承**这类磨损型退化部件为参照对象；信号是磨损量(mm)与温度(℃)；失效判据是磨损量达 1.0mm；输出落到运维检修决策 |
| 「SQLite 算数据库吗？」 | 是完整的关系型数据库（支持 SQL、事务、索引），本场景单机演示零配置更合适；扩展多用户时迁移 MySQL 即可 |
| 「智能搜索体现在哪？」 | 两处：①超参网格搜索在 `hidden_size×lr` 状态空间中以验证集 MSE 择优（实际影响训练）；②RUL 估算用滚动前向状态空间搜索失效时刻 |
| 「仿真数据 0.4% 的误差是不是太容易了？」 | 是，这正是选仿真的代价——噪声为高斯模型，比真实工况简单。所以我在选型论证里主动说明了这一点，并把 C-MAPSS 验证列为展望 |
| 「业务闭环体现在哪？」 | 预警不只输出等级，还输出**可执行检修建议**（计划检修/提前备件/立即停机），前端醒目展示，并按等级归档到 alerts 表供追溯 |
