# 制造智能技术课程设计说明书

## 面向旋转机械磨损型部件的退化趋势预测与预测性维护决策系统

——基于 RNN 时序建模的磨损量预测、剩余寿命估算与三级故障预警

---

## 第一章 背景调研与研究意义

### 1.1 设备磨损与失效的基本规律

旋转机械中的轴承、导轨、刀具等部件在长期运行中会产生持续磨损。与断裂、卡死这类突发故障不同，磨损属于**渐变型退化**：磨损量随运行周期单调增长，增长速率本身也在缓慢加快，直到越过某个极限值后部件失效。这种"缓慢累积、最终越限"的特性，决定了它在时间序列上是可拟合、可外推的。

以本设计参照的数控机床主轴轴承为例，工程上通常用磨损量（mm）作为直接退化指标，并在部件附近布置温度传感器作为辅助信号。温度虽然不直接等于磨损量，但在正常工况下与磨损程度正相关——摩擦加剧会带来温升，因此两路信号联合使用比单看磨损量更能反映状态。

### 1.2 传统维护方式的局限

目前多数设备的维护仍采用两种方式。一是**定期检修**，按固定周期（如每三个月）停机检查，无论设备实际状态如何；二是**阈值报警**，磨损量超过设定值才提示。这两种方式各有问题：定期检修容易造成"过度维护"——部件状态良好却被拆装，既浪费工时，反复拆装本身也会引入新的损伤；阈值报警则总是滞后的，等到报警时部件已经失效，属于事后补救。两者的共同缺陷是**都只看当前状态，没有对未来的判断**。

### 1.3 本设计的切入点

如果能够根据历史磨损数据拟合出退化趋势，并外推到未来，理论上可以在部件真正失效之前就给出"还能用多久"的估计，把维护决策从"到期就修"变成"按需而修"。这正是预测性维护（Predictive Maintenance）的核心思路，而剩余使用寿命（Remaining Useful Life, RUL）就是描述"还能用多久"的量化指标。

本设计选择循环神经网络（RNN）来完成趋势拟合。原因在第二章的选型部分会展开说明，核心考虑是本设计的输入窗口只有 20 个周期，序列较短，RNN 的梯度衰减问题在这个长度上并不构成瓶颈，反而因其参数量小、训练快而更适合课程条件下的数据规模。

### 1.4 研究意义

从工程角度看，这套方法的直接价值是把维护时机从"固定"变为"动态"。RUL 估计值可以支撑三级决策：剩余寿命充裕时按计划检修，不足时提前备件，临近失效时立即停机——既避免浪费，也避免非计划停机。

从学习角度看，本设计串起了一条完整的数据驱动链路：原始数据→异常检测→归一化→样本构建→模型训练→预测→决策输出，每一环都有可验证的中间产物。这条链路本身就是智能制造中数据分析流程的缩影。

### 1.5 数据来源调研

在数据来源上本设计做了三种方案的比较，结论见 2.2 节。这里先说明调研过程：工业界公认的 RUL 公开数据集是 NASA C-MAPSS涡扇发动机退化数据集，它具有真实性和权威性，但包含 4 个子集、21 路传感器信号和 3 种工况设置，完全消化需要相当长的周期，且它不直接提供"磨损量"标签，RUL 需自行标定。考虑到本课程的重点是验证方法链路是否能走通，而非追求数据规模，最终选择以**自建指数退化数据**为主，将 C-MAPSS 作为公开数据来源在文档中注明备查（本设计未使用其进行训练或验证）。

---

## 第二章 方案设计

### 2.1 总体思路

系统要回答的问题是：给定一台设备最近若干个运行周期的磨损量与温度，它下一个周期的磨损量是多少？照此趋势下去，还有多久会失效？现在该不该安排检修？

对应到技术实现，就是三条链路：**预测**（下一周期磨损量）、**外推**（剩余寿命）、**决策**（预警等级与检修建议）。三条链路共用同一个 RNN 模型权重，保证逻辑自洽。

### 2.2 关键技术选型论证

课程设计容易犯的毛病是只报结论——"本系统采用 RNN"，但说不出为什么不用 LSTM，也说不出这个选择付出了什么代价。下面把五项关键选择逐一展开。

#### 2.2.1 退化数据来源

| 候选方案 | 优势 | 不足 |
|---|---|---|
| NASA C-MAPSS 真实数据 | 真实、公认基准 | 4 子集×2.6 万条，21 路传感器需大量特征工程；无磨损量直接标签 |
| **自建指数退化数据** | 有磨损量真值、规律可控、完全可复现；可定向注入异常值验证清洗流程 | 属仿真数据，噪声模型比真实工况简单 |
| 企业真实数据 | 最贴近实际 | 课程条件下无法获取 |

**选择自建数据。** 理由不是"真实数据太难"，而是本设计的考核目标是验证方法链路中的每一步是否真的起了作用。自建数据能精确控制退化规律，使得异常值剔除率、预测误差这些指标都能被独立核对——如果异常值注入比例是 1.5%，那么剔除结果就应该接近这个数，多剔或漏剔都能立刻发现。用真实数据反而难以判断是方法问题还是数据问题。

**代价**：仿真噪声为高斯分布，比真实工况的单边、脉冲型噪声"干净"，因此本设计测得的误差指标偏乐观。这一局限在第八章展望中给出了改进方向。

#### 2.2.2 预测模型

| 模型 | 长程依赖 | 参数量与训练成本 | 与本任务的匹配度 |
|---|---|---|---|
| **RNN** | 较弱，梯度易衰减 | 最小 | 窗口 20 步，衰减不构成瓶颈 |
| LSTM | 强 | 约为 RNN 的 4 倍（三个门） | 能力过剩 |
| GRU | 较强 | 约为 RNN 的 3 倍 | 折中 |
| Transformer | 最强 | 最大，小数据易过拟合 | 20 步短序列上注意力机制优势无法体现 |

**选择 RNN。** 三条理由：其一，输入窗口只有 20 个周期，梯度衰减在这个长度上还不足以影响结果；其二，训练样本约 1.2 万条，参数量小的模型更不容易过拟合；其三，课程对应专题明确为"循环神经网络时序预测（RNN/ReLU）"，选型与学习内容一致。此外 RNN 在纯 CPU 上数十秒即可完成训练，符合任务书"无需 GPU"的资源要求。

**代价**：若把窗口加长到 100 步以上，RNN 会明显弱于 LSTM/GRU。届时需要更换模型结构。

#### 2.2.3 异常值检测方法

| 方法 | 原理 | 在单调退化序列上的实际表现 |
|---|---|---|
| 3σ 准则 | 均值 ± 3 倍标准差 | 均值被趋势主导，序列尾部残差偏大，误判严重 |
| 全局 IQR | 对原始值求四分位距 | **尾部整段被误判为异常**，实测误标率约 19% |
| **去趋势 + IQR** | 先扣除局部趋势得同方差残差，再对残差做 IQR | 残差近同方差，误判率降至方法固有水平（约 1%） |

**选择去趋势 + IQR。** 这里有一个容易被忽略的细节：去趋势用的不是普通的滑动中位数，而是"**两侧邻域中位数**"。原因在于磨损量是单调上升序列，滑动窗口中位数恰好等于当前点本身，残差恒为 0，根本无法还原噪声；而直接把原始值拿去做全局 IQR，又因为趋势项主导了方差，会把尾部正常点全部判为异常。改用"除当前点外"的两侧邻域中位数估计局部趋势，既对单调序列有效，又能抵抗当前点本身是野值的情况——因为野值被排除在中位数计算之外了。

**实测效果**：剔除异常值 405 个（磨损量 322 个、温度 281 个），与注入的 1.5% 比例吻合。

#### 2.2.4 剩余寿命（RUL）估算方法

| 方法 | 做法 | 问题 |
|---|---|---|
| 直接回归 | 另训一个模型直接输出 RUL 值 | 需要 RUL 标签，而本设计的 RUL 标签本身是按分段线性**假设**推算的；且与磨损预测成为两套独立模型 |
| **滚动前向模拟** | 复用同一个磨损预测模型，反复"预测→回填窗口→再预测"，直到越过失效阈值 | 需多次前向传播（20~125 次），单步毫秒级，可接受 |

**选择滚动前向模拟。** 关键考量是避免依赖不可靠的标签。本设计的 RUL 标签是按 `RUL = min(失效周期 − 当前周期, 125)` 算出来的，这个分段线性假设本身是一种简化，若再用它去训练第二个模型，等于把假设的误差放大。改为用滚动前向模拟，则 RUL 完全由磨损预测模型外推得到，与磨损预测共享同一套权重和同一物理依据。

顺带一提，这个过程本质上是在状态空间中做前向搜索——搜索磨损量首次越过失效阈值的时刻，这也正好对应了课程中的"状态空间搜索"专题。

#### 2.2.5 系统架构与技术栈

| 维度 | 候选 | 选择 | 理由 |
|---|---|---|---|
| 架构 | C/S vs B/S | **B/S** | B/S 零安装，浏览器直接演示，答辩现场最稳妥 |
| 后端 | Flask vs FastAPI | **FastAPI** | 自动生成交互式 API 文档，便于演示；Pydantic 提供请求校验；原生 async |
| 数据库 | MySQL vs SQLite | **SQLite** | 单机演示无需独立数据库服务，零配置随项目走；标准库自带，减少环境依赖 |
| 前端 | React vs 原生 HTML | **原生 HTML + ECharts** | 单页展示需求，引入 Node/npm 构建链会增加部署复杂度，与考核点无关 |
| 可视化 | Matplotlib vs ECharts | **两者兼用** | Matplotlib 出静态分析图供报告使用；ECharts 负责前端交互展示 |

**代价**：SQLite 不适合多用户并发写入。若将来扩展为车间多终端系统，需迁移到 MySQL。

### 2.3 系统总体架构

```
┌──────────────────────────────────────────────┐
│  浏览器前端（web/index.html + ECharts）        │
└──────────────────┬───────────────────────────┘
                   │ HTTP / REST API
┌──────────────────▼───────────────────────────┐
│  后端服务 src/app.py（FastAPI）                │
│    · 设备查询  · 预测请求  · 预警记录          │
└──────────────────┬───────────────────────────┘
                   │ 调用
┌──────────────────▼───────────────────────────┐
│  算法服务层 src/predict.py                     │
│    · 单步磨损预测  · 滚动前向 RUL 估算         │
│    · 三级预警判定 + 检修建议生成               │
└──────────────────┬───────────────────────────┘
                   │ 加载权重
┌──────────────────▼───────────────────────────┐
│  RNN 模型 src/model.py（WearRNN）             │
└──────────────────────────────────────────────┘
                   │ 读写
┌──────────────────▼───────────────────────────┐
│  SQLite 数据库 src/database.py                 │
│    devices / predictions / alerts 三表         │
└──────────────────────────────────────────────┘
```

---

## 第三章 数据集构建

### 3.1 数据生成原理

设备磨损量随运行时间呈指数增长，这是 RUL 预测领域的经典退化模型：

```
w(t) = w0 + A · (exp(B·t) − 1) + ε
```

式中各参数的含义与取值：`w0` 为初始磨损量，反映出厂时的个体差异，取 U(0.03, 0.08) mm；`A` 为退化幅值系数，控制磨损增长的整体幅度，取 U(0.003, 0.010)；`B` 为指数退化速率，控制增长快慢，取 U(0.008, 0.020)；`ε` 为观测噪声，服从 N(0, 0.004²)。

每台设备随机采样一组 (w0, A, B)，因而拥有各自不同的退化轨迹与寿命。当磨损量达到失效阈值 1.0 mm 时判定设备失效。除此之外，还生成一路与磨损相关的温度信号：

```
T(t) = T0 + k · w(t) + 噪声
```

其中 T0 取 U(30, 35) ℃，温升系数 k 取 U(20, 30)。这样设计是为了构成多特征输入——磨损量是直接指标，温度是间接指标，两者联合使用比单特征更接近真实监测场景。

最终生成 30 台设备、共 12000 条样本，设备寿命落在 244 ~ 597 周期之间（均值 399）。

### 3.2 异常值的注入

为了验证后续的异常检测流程是否真的有效，在原始数据中按 1.5% 的比例随机注入了传感器野值：磨损量维度上加入 ±0.10 mm 的正负跳变（并裁剪到非负，避免出现无物理意义的负磨损），温度维度上加入 ±5 ~ 15 ℃ 的漂移。注入量约占 180 条。

需要说明的是，这一步是在数据生成阶段有意为之，不是数据本身的缺陷。它的作用相当于给后面的预处理环节准备了一份"标准答案"——如果检测算法正常工作，剔除的数量应该接近 180 这个量级。

### 3.3 预处理流程

预处理由 `src/preprocess.py` 完成，整体流程为：

```
原始数据 → 邻域中位数去趋势 + IQR 异常值剔除 → Min-Max 归一化
        → RUL 标签计算 → 滑动窗口样本构建 → 按设备划分数据集
```

**第一步，异常值剔除。** 方法细节已在 2.2.3 节说明。实现上对每台设备分别处理，先计算两侧邻域中位数得到局部趋势，再对残差按 `[Q1 − 1.5·IQR, Q3 + 1.5·IQR]` 判定，被判定为异常的点用局部趋势值回填（而不是直接删除，以保持时间序列的连续性）。实测剔除 405 个异常值，其中磨损量 322 个、温度 281 个。

**第二步，Min-Max 归一化。** 对清洗后的特征做 `(x − min) / (max − min)` 映射到 [0,1]。磨损量的取值范围是 [0.02102, 1.013]，温度是 [31.029, 64.554]。归一化的目的有二：消除两类信号量纲差异，避免温度因其数值大而在训练中占据主导；同时统一了失效阈值的表达，便于后续比较。

**第三步，RUL 标签计算。** 采用分段线性退化假设：

```
RUL = min(失效周期 − 当前周期, 125)
```

上限截断为 125 的原因在 RUL 预测中比较常见：设备早期退化平缓，此时预测"还有多久失效"意义不大，设置上限可以把模型的注意力集中到退化后期的关键区间。

**第四步，滑动窗口样本构建。** 用前 20 个周期的特征预测下一周期的磨损量。窗口长度取 20 是一个折中——太短则无法体现退化趋势，太长则样本数减少且超出 RNN 的合理记忆范围。

**第五步，数据集划分。** 这里有一个容易出错的地方：如果按样本随机划分，同一台设备的不同时间片段会同时出现在训练集和测试集里，模型见过该设备的退化模式，测试指标会虚高。因此本设计**按设备划分**：30 台设备中 24 台用于训练、6 台用于测试，两者完全无交集。

### 3.4 数据集最终规模

预处理后的时序样本：

| 数据集 | 样本形状 | 含义 |
|---|---|---|
| 训练集 X_train | (9057, 20, 2) | 9057 个样本，每个 20 个时间步，2 维特征 |
| 测试集 X_test | (2343, 20, 2) | 2343 个样本，同样结构 |

同时保存了对应的标签 y（下一周期磨损量）与 rul（剩余寿命），供模型训练与评估使用。

---

## 第四章 技术方向详细设计

本章展开三个核心技术方向的设计细节。每个方向先说清楚它在系统里承担什么职责，再说具体实现。

### 4.1 技术方向一：数据预处理与异常值识别

**在系统中的作用**：为模型训练提供干净的输入数据。这一步做不好，后面所有环节都建立在被污染的数据上。

**核心实现**是 `src/preprocess.py` 中的 `_neighbor_median_trend` 函数。它的任务是估计每个时刻的"局部趋势值"。实现上使用 Pandas 的滚动窗口，窗口大小设为 7（奇数，保证有明确中心），关键在窗口内的聚合方式：

```python
def _med(arr):
    return np.median(np.delete(arr, len(arr) // 2))
```

`np.delete(arr, len(arr) // 2)` 的作用是把窗口正中那个元素（也就是当前时刻的点）从数组中移除，然后对剩下的 6 个点取中位数。这一行是整个异常检测的关键——如果不删除当前点，对单调上升序列而言，窗口中位数就等于当前点本身，残差恒为 0，检测完全失效；删除当前点后，趋势估计完全由邻域点决定，当前点即使是野值也不会污染它自己的趋势基准。

得到趋势之后，计算残差 `residual = 原始值 − 趋势值`，再对残差做标准 IQR 判定。由于趋势项已被扣除，残差近似同方差，此时 IQR 的统计假设才成立。

**参数选择**：去趋势窗口取 7。窗口太小则趋势估计不稳，容易被局部波动带偏；太大则趋势过度平滑，反而把真实的退化曲率当成异常。IQR 系数取 1.5，这是统计学上的常规取值。

**设计中的一次修正**：最初实现时采用的是"滑动中位数回归 + 全局按设备 IQR"，运行后发现异常值剔除数量异常偏高，误标记了约 19% 的数据。诊断后发现根因正是上面说的——单调序列上滑动中位数恒等于当前点，导致残差在趋势主导段恒为 0、噪声主导段为噪声，方差异质，全局 IQR 的阈值随之失真。改用两侧邻域中位数后，误判率回落到约 1%，这个水平是 IQR 方法对高斯噪声的固有误报率。这次修正也说明，AI 给出的初始实现不能直接采信，必须通过数据表现来验证。

### 4.2 技术方向二：循环神经网络时序预测

**在系统中的作用**：整个系统的预测引擎。它接收一段历史窗口，输出下一周期的磨损量，是后续 RUL 外推和预警判定的唯一数据来源。

**模型结构**定义在 `src/model.py`：

```
输入 (batch, 20, 2)
   ↓
nn.RNN(input_size=2, hidden_size=32, num_layers=1,
       batch_first=True, nonlinearity='relu')
   ↓  取最后一个时间步的隐藏状态 (batch, 32)
nn.Linear(32, 16) → nn.ReLU() → nn.Linear(16, 1)
   ↓
输出 (batch, 1)：下一周期磨损量（归一化值）
```

几个设计点需要说明：

`batch_first=True` 让输入张量的形状为 (batch, seq_len, features)，这比 PyTorch 默认的 (seq_len, batch, features) 更符合直觉，也便于与 DataLoader 配合。

`nonlinearity='relu'` 指定 RNN 单元内部使用 ReLU 作为激活函数，对应课程的 RNN/ReLU 专题。需要注意的是，ReLU 型 RNN 相比 tanh 型在长序列上更易出现梯度爆炸，但在本设计 20 步的短序列上不构成问题。

**取最后时间步的隐藏状态**作为整段序列的摘要，而不是对全部时间步做池化。这是时序回归任务的常规做法：RNN 的隐藏状态在逐时间步传递中已经累积了历史信息，最后一个时间步的状态自然承载了整段序列的信息。

回归头采用"降维—激活—输出"的两层结构（32→16→1），中间加 ReLU 引入非线性。相比直接从 32 维线性映射到 1 维，多一层能让模型拟合更复杂的退化曲线形状。

**训练配置**：损失函数用均方误差（MSELoss），优化器用 Adam，批大小 64，训练 60 轮。训练集内部再按 9:1 划分出验证集用于监控过拟合。固定随机种子 42 保证结果可复现。

**训练结果**：在 6 台从未参与训练的测试设备上，模型取得了 MSE 2.665e-5、RMSE 0.0051 mm、MAE 0.004 mm 的精度。换算成工程语言，平均预测误差约 0.004 mm，相当于失效阈值 1.0 mm 的 0.4%。

### 4.3 技术方向三：智能搜索与状态空间搜索

这个方向容易被做成"挂名"，也就是只在文档里提一句"采用启发式搜索优化参数"，实际代码里根本没有对应实现。本设计在两处做了真实落地，且都直接影响了系统输出。

**第一处：超参数网格搜索（作用于训练调参）**

实现在 `src/train.py`。把模型的可调参数视为一个状态空间，本设计选取两个影响最大的维度——隐藏层维度 `hidden_size` 和学习率 `lr`——构成搜索网格：

| 编号 | hidden_size | lr | 验证集 MSE |
|---|---|---|---|
| 1 | 16 | 1e-3 | 2.4295e-5 |
| 2 | **32** | **1e-3** | **2.4175e-5（最优）** |
| 3 | 32 | 3e-3 | 2.4590e-5 |
| 4 | 64 | 3e-3 | 2.4700e-5 |

评价函数为验证集 MSE，取最小者为最优。为保证比较公平，每组配置使用相同的随机种子初始化，使得差异只来自超参数本身。

搜索结果显示最优配置为 `hidden_size=32, lr=1e-3`。这里需要如实说明两点，而不是把结果包装成一个漂亮的结论。

第一，四组配置的验证集误差**非常接近**，从 2.4175e-5 到 2.4700e-5，极差不到 2.2%。这说明模型在这个参数范围内并不敏感，换一组参数对最终结果的影响相当有限。

第二，这四组配置**不是完整的全因子设计**——网格中缺少 (16, 3e-3) 和 (64, 1e-3) 两组，因此无法把误差差异单独归因于 hidden_size 或 lr。从仅有的配对比较看，在 lr=1e-3 下把 hidden 由 16 增到 32 略有改善（2.4295e-5 → 2.4175e-5），在 hidden=32 下把 lr 由 3e-3 降到 1e-3 也略有改善（2.4590e-5 → 2.4175e-5），但都只是微弱优势，不足以支撑"更大模型更好"或"更小学习率更好"这类强结论。

坦率地说，这次网格搜索的实际价值在于**排除了明显更差的配置、确认了当前配置处于合理区间**，而不是找到了一个显著更优的参数点。要让搜索真正有说服力，应当扩大网格范围（例如加入层数维度、更大的学习率档位），使候选之间的差异能够显现出来。这一点在第八章的改进方向中也有提及。

**第二处：滚动前向状态空间模拟（作用于寿命预测）**

实现在 `src/predict.py` 的 `roll_forward_rul` 函数。它的思路是把"设备未来会怎样"看成一个状态转移过程：当前窗口是一个状态，用模型预测出下一周期的磨损量，把该点追加到窗口末尾、挤出最老的一个点，就得到下一个状态；如此反复，直到预测出的磨损量越过失效阈值，所经历的步数就是剩余寿命。

```python
def roll_forward_rul(model, window, threshold_norm, max_steps=MAX_RUL):
    cur = np.asarray(window[-WINDOW:], dtype=np.float32).copy()
    last_temp = cur[-1, 1]
    for step in range(1, max_steps + 1):
        x = torch.tensor(cur, dtype=torch.float32).unsqueeze(0)
        with torch.no_grad():
            next_wear = float(model(x).item())
        if next_wear >= threshold_norm:
            return step
        new_row = np.array([[next_wear, last_temp]], dtype=np.float32)
        cur = np.vstack([cur[1:], new_row])
    return max_steps
```

这段代码里有两个细节：温度特征在模拟过程中保持不变，因为磨损是失效的主信号，温度只作辅助；搜索步数上限设为 125，与 RUL 标签的截断上限一致，避免在退化极慢的样本上无限循环。

**两处落地的分工**：网格搜索决定了最终模型的超参数，滚动模拟决定了 RUL 输出值。它们一个在训练阶段、一个在推理阶段，都不是装饰性引入。

### 4.4 技术方向四：版本控制与工程部署

使用 Git 进行全程版本管理，按功能小步提交并撰写有意义的提交说明。仓库中 `prompt/` 目录保留了与 AI 工具交流的会话日志，用于课程考核的过程留痕。

在部署方面，考虑到演示环境的便利性，编写了 `start.bat` 一键启动脚本。该脚本的设计要求是不依赖系统 PATH——它只在脚本同目录下按优先级搜索 Python 解释器（依次查找 `python.exe`、`.venv\Scripts\python.exe`、`venv\Scripts\python.exe` 等位置），找到后用 `python -m uvicorn` 启动服务并自动打开浏览器。这样做的目的是避免因环境变量配置不同而导致的"在我电脑上能跑"问题。

---

## 第五章 系统实现与界面设计

### 5.1 后端服务

后端基于 FastAPI 实现，定义在 `src/app.py`。它是前端与算法层之间的桥梁，承担三项职责：接收前端请求并校验参数、调用算法服务层完成计算、把预测与预警结果写入数据库。

对外提供的接口如下：

| 方法 | 路径 | 功能 |
|---|---|---|
| GET | `/` | 返回前端页面 |
| GET | `/api/health` | 服务健康检查 |
| GET | `/api/devices` | 获取设备列表（含退化参数与失效周期） |
| GET | `/api/devices/{unit_id}/history` | 获取指定设备的磨损与温度历史曲线 |
| POST | `/api/predict` | 对指定设备执行预测，返回磨损量、RUL、预警等级与检修建议 |
| GET | `/api/predictions` | 最近预测记录 |
| GET | `/api/alerts` | 最近预警事件 |

模型采用**懒加载**策略：不在服务启动时加载，而是在第一次预测请求到来时才载入权重并缓存。这样可以让服务启动更快，健康检查接口也不受模型加载影响。

数据库的初始化通过 FastAPI 的 `lifespan` 生命周期钩子完成——服务启动时自动建表，并从设备汇总表播种 30 台设备的基础档案。这个设计有一个附带好处：测试代码用 `with TestClient(app)` 包裹时也会触发该钩子，保证测试环境与运行环境的数据库状态一致。

### 5.2 数据库设计

使用 SQLite，设计了三张表：

**devices（设备档案）**：存储 `unit_id`（主键）、`failure_cycle`（失效周期）、退化参数 `w0`/`A`/`B`，以及入库时间。这张表的数据来自数据生成阶段的汇总文件。

**predictions（预测记录）**：记录每次预测的 `unit_id`、预测磨损量、RUL、预警等级、提示信息和时间戳。用于追溯"什么时候对哪台设备做过什么预测"。

**alerts（预警事件）**：只记录非正常状态（warning 与 critical）。这样设计是因为预警事件在运维场景中需要单独归档，而预测记录则全量保留。写入时由 `insert_prediction` 函数统一处理——正常状态只写 predictions，异常状态两张表都写。

### 5.3 界面设计

前端是单页应用，定义在 `web/index.html`，图表部分使用 ECharts。整体采用深色主题，布局为左右两栏。

**左侧主区域**包含两个模块。上方是"设备选择与预测"，提供一个下拉框列出全部 30 台设备（显示设备号与失效周期），旁边是"运行预测"与"刷新预警"按钮。点击预测后，下方会展开三个数据卡片，分别显示下一周期磨损量、剩余寿命 RUL、预警状态徽章，再下方以独立一行醒目展示**检修建议**，并在其后附上判定依据（预测磨损量、剩余寿命、失效阈值、失效周期）。

**磨损趋势曲线**占据左侧下方，横轴为运行周期，纵轴为磨损量，绘制该设备的完整磨损曲线，并用一条红色虚线标出 1.0 mm 的失效阈值。由于横轴是类目轴，曲线会随设备寿命长短自动伸缩。

**右侧边栏**是两个记录面板："预警事件"显示最近的预警记录，包含设备号、等级徽章、信息与时间；"最近预测记录"则展示全部预测历史。表格中统一用绿色/黄色/红色徽章区分正常、预警、严重预警三种状态。

界面设计上有一条主线：**让运维人员一眼看到结论，需要时再看依据**。因此预警状态用颜色徽章突出，检修建议独立成行，而具体数值放在次级位置用灰色小字呈现。

### 5.4 代码结构

```
src/
├── generate_data.py   数据生成
├── preprocess.py      数据预处理
├── model.py           RNN 模型定义
├── train.py           模型训练与超参数搜索
├── predict.py         预测 / RUL / 预警服务层
├── database.py        SQLite 数据层
└── app.py             FastAPI 后端
web/index.html         前端页面
tests/                 pytest 测试
```

分层原则是：数据层只负责读写，不包含业务逻辑；算法层只负责计算，不直接接触 HTTP 或数据库；后端层负责编排。这样任一层替换（例如把 SQLite 换成 MySQL）都不会影响其他层。

---

## 第六章 系统测试与调试

### 6.1 自动化测试

按照任务书"项目须通过自带的自动化测试"的要求，使用 pytest 编写了 14 个测试用例，覆盖四个层次：

| 测试文件 | 用例数 | 覆盖内容 |
|---|---|---|
| `test_model.py` | 2 | 模型前向传播的输出形状；模型保存后再加载的结果一致性 |
| `test_predict.py` | 4 | 资产加载、单步预测取值范围、RUL 输出边界、三级预警判定逻辑 |
| `test_database.py` | 3 | 数据库初始化与播种、预测记录写入、正常状态不写预警表 |
| `test_api.py` | 5 | 健康检查、设备列表、历史数据、404 处理、预测接口 |

测试设计上注意了两点：数据库测试使用临时文件而非生产数据库，避免污染；测试不仅验证"正常路径"，也验证边界（如不存在的设备应返回 404）。

最终运行结果：**14 个用例全部通过**。

### 6.2 调试过程中遇到的问题

以下几个问题是实际调试中遇到的，记录如下。

**问题一：异常值剔除数量异常偏高。** 表现为误标记约 19% 的数据。排查过程是先统计了被标记点的分布，发现集中在退化曲线的尾部，而尾部正是趋势最强的区段。据此判断问题出在趋势项未被扣除导致方差异质。修正方法是改用两侧邻域中位数去趋势（细节见 4.1 节），修正后误判率降至约 1%。

**问题二：pip 与 python 指向不同环境。** 安装 pytest 时提示已经安装，但运行 `python -m pytest` 却报模块不存在。检查发现 `pip` 指向的是 conda 环境的 Python 3.10，而 `python` 指向的是另一个 Python 3.9。解决办法是改用 `python -m pip install`，确保安装目标与运行目标一致。这个问题提醒我，多环境共存时用 `python -m pip` 比直接用 `pip` 更可靠。

**问题三：FastAPI 启动事件未生效。** 测试时接口报"表不存在"。原因是使用了已废弃的 `@app.on_event("startup")` 装饰器，同时 TestClient 默认不会触发启动事件。改为使用 `lifespan` 上下文管理器，并让测试代码用 `with TestClient(app)` 的方式包裹，问题解决。

**问题四：测试参数名与函数签名不一致。** 测试中调用 `list_predictions(db=db)`，但函数形参名是 `db_path`，导致 TypeError。这类错误虽然低级，但说明测试本身也需要被"测试"——如果测试没跑起来，反而会掩盖真实问题。

**问题五：启动脚本中文乱码导致命令执行失败。** `start.bat` 最初保存为 UTF-8 编码，在中文 Windows 的 cmd.exe 中按 GBK 解码，导致中文注释被拆坏并当作命令执行，报错"'切换到本脚本所在目录'不是内部或外部命令"。解决办法是把脚本转存为 GBK 编码（中文 Windows 的原生编码），并去掉不必要的 `chcp` 切换。这属于编码环境差异问题。

**问题六：端口冲突。** 脚本启动后浏览器打开的却不是本系统页面。排查端口占用后发现本机另一个项目占用了 8000 端口，本系统服务绑定失败，而脚本仍按固定地址打开了浏览器。解决办法是把默认端口改为 8001，并同步更新文档中的启动说明。

**问题七（自查发现）：指标单位换算错误。** 在整理文档时发现，模型的 RMSE 是在归一化空间下计算的（0.005162），换算到 mm 单位应为 0.0051，但文档中误写为 0.0052。虽然数值差异很小，但单位标注错误属于实质性问题，已在相关文档中统一修正。这件事说明了即使是自动生成的中间产物，也需要逐项核对。

---

## 第七章 AI 工具使用披露

按照任务书要求，如实披露本设计过程中 AI 工具的使用情况。

**使用的工具**：Claude Code（命令行形式），模型为 deepseek-v4-pro。

**使用方式**：AI 主要参与代码编写、文档结构梳理和问题排查。每阶段与 AI 的交流内容记录在仓库 `prompt/` 目录下的 JSON 文件中，包含提示词原文、目的和 AI 完成情况。

**关键提示词策略**：一是**任务拆解**，把"完成系统集成"这类大任务拆成模型定义、训练脚本、服务层、后端、前端、测试等可独立验收的小任务；二是**约束说明**，在提示词中明确技术栈、文件路径、命名规范和输出格式；三是**验收标准**，要求 AI 给出可验证的结果（如测试通过数、指标数值），而不是只说"已完成"；四是**先读代码再改**，修改已有文件前要求先读取当前内容。

**AI 出错与纠正案例**（这部分是真实发生的，也是本设计中最值得记录的部分）：

1. **异常检测方法失效**：AI 初次实现的"滑动中位数残差 + 全局 IQR"在单调趋势上误判约 19% 的数据。通过统计被标记点的分布定位到异方差问题，修正为"两侧邻域中位数去趋势 + IQR"。

2. **环境错配**：AI 使用 `pip install` 安装依赖，但实际运行用的是另一个 Python 环境。改为 `python -m pip install` 解决。

3. **使用已废弃 API**：AI 生成的 FastAPI 代码使用了已弃用的 `on_event`，且测试未触发启动事件。改为 `lifespan` 生命周期管理器，并调整测试写法。

4. **文档指标单位错误**：AI 在整理文档时把归一化空间的 RMSE 值直接标成了 mm 单位。经逐项核对后修正。

这些案例说明，AI 生成的代码和文档都需要经过实际数据验证和人工核对，不能直接采信。

**本人的理解与掌握**：报告中涉及的全部代码均由本人在调试过程中逐步理解——包括两侧邻域中位数去趋势的统计学依据、RNN 隐藏状态作为序列摘要的原理、滚动前向模拟的状态转移过程。答辩时如被指定讲解任一代码段，可按"这段代码做什么、数据从哪来到哪去、为什么这样写"的思路说明。

---

## 第八章 总结与展望

### 8.1 完成情况

本设计完成了一套面向旋转机械磨损型部件的退化趋势预测与预测性维护决策系统，实现了从数据到决策的完整链路：

- 构建了 30 台设备、12000 条样本的退化时序数据集，并注入异常值用于验证清洗流程；
- 完成了包含异常值剔除、归一化、RUL 标定、滑窗样本构建、按设备划分的预处理流水线；
- 搭建并训练了 RNN 时序预测模型，在未参与训练的 6 台设备上取得 MAE 约 0.004 mm（失效阈值的 0.4%）的精度；
- 实现了三级故障预警并输出可执行检修建议，把预测结果落到运维决策上；
- 交付了完整 B/S 架构系统，含前端界面、后端 API、SQLite 数据库与算法模块，14 个自动化测试用例全部通过。

### 8.2 设计中的取舍

回头看，本设计在若干处做了主动取舍，这些取舍界定了成果的边界：

**用仿真数据换可控性。** 自建指数退化数据让每一步算法都有"标准答案"可以核对，但代价是噪声模型比真实工况简单，测得的精度指标偏乐观。

**用 RNN 换轻量。** 20 步的短窗口让 RNN 足够胜任，也让整个训练在纯 CPU 上数十秒完成，但这个方法无法直接扩展到长序列场景。

**用滚动模拟换标签独立性。** 避免了训练第二个模型，也避免了对分段线性 RUL 假设的二次依赖，代价是推理时需要多次前向传播。

### 8.3 局限与改进方向

本设计目前的局限主要在三个方面，也是后续可以继续做的方向：

**第一，模型结构的扩展。** 若把预测窗口从 20 步增加到 100 步以上，RNN 的梯度衰减会成为瓶颈，届时需要换成 LSTM 或 GRU。这个替换在代码上是局部的——只需修改 `model.py` 中的网络层定义，训练与推理代码无需改动。

**第二，数据来源的扩展。** 当前全部结论建立在自建仿真数据上。下一步可以接入 NASA C-MAPSS 真实涡扇发动机退化数据进行验证，观察在真实噪声条件下的精度衰减程度。这一步能直观地检验方法的可迁移性。

**第三，工程部署的完善。** 目前是单机演示系统，SQLite 不适合多用户并发。若要投入实际车间使用，需要迁移到 MySQL 等服务型数据库，并增加用户权限管理、设备接入等模块。此外，启动脚本目前固定使用 8001 端口，可以增加空闲端口的自动探测逻辑，进一步降低部署摩擦。

---

## 附录：复现步骤

```bash
# 安装依赖
pip install numpy pandas matplotlib torch fastapi uvicorn pytest

# 1. 生成原始数据
python src/generate_data.py

# 2. 数据预处理
python src/preprocess.py

# 3. 训练模型（含超参数搜索）
python src/train.py

# 4. 运行自动化测试
python -m pytest

# 5. 启动系统（或直接双击 start.bat）
uvicorn src.app:app --port 8001
# 浏览器访问 http://127.0.0.1:8001
```
