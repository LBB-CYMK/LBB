# 选型论证与选题细化

> 针对评审意见「**选型和选题未具体**」的专项补充材料。
> 核心思路：把「我用了 X」升级为「我在 A/B/C 中为什么选 X，代价是什么，验证数据是多少」。

---

## 第一部分　选题细化：把「设备磨损预测」收敛到具体场景

### 1.1 问题：选题过于宽泛

原标题「基于深度学习的设备磨损趋势预测与故障预警系统」缺少：
- 具体是**什么设备、什么工况、什么失效模式**
- 数据的**物理量纲与来源依据**
- 系统的**使用角色与业务闭环**（谁用、什么时候用、用完做什么决策）

### 1.2 收敛后的选题表述

> **面向旋转机械（以数控机床主轴轴承为参照对象）的磨损趋势预测与预测性维护决策系统**
> ——基于多传感器时序（磨损量 + 温度）的 RNN 退化建模，输出剩余寿命 RUL 与三级预警，
> 为设备运维人员提供「何时停机检修」的决策依据。

### 1.3 场景三要素具体化

| 要素 | 具体化内容 |
|---|---|
| **对象** | 旋转机械的磨损型退化部件（主轴轴承 / 导轨 / 刀具），失效模式为**磨损失效**（磨损量越过阈值） |
| **工况与信号** | 按运行周期采集两类信号：**磨损量 w(t)**（mm，直接退化指标）+ **温度 T(t)**（℃，与磨损强相关的间接信号，用于多特征输入） |
| **失效判据** | 磨损量达到 **1.0 mm** 判定失效（工程上常见的磨耗极限取值） |
| **业务闭环** | 运维人员看板 → 查看各设备当前磨损与预测趋势 → 系统给出 RUL 与预警等级 → 据此安排检修计划（正常：计划性检修；预警：提前备件；严重：立即停机） |

### 1.4 为什么这个场景值得做

- **失效可预见**：磨损是**渐变型退化**（非突发故障），退化轨迹有规律可拟合 → 适合时序预测
- **预测有收益**：提前知道 RUL 可把「定期检修」变为「按需检修」，减少非计划停机
- **数据可获取**：退化过程可用经典退化模型刻画，也确有公开真实数据可对照（NASA C-MAPSS）

---

## 第二部分　选型论证：五组关键技术选型

### 2.1 选型一：退化数据来源 —— 自建仿真 vs 公开数据集

| 方案 | 优点 | 缺点 | 结论 |
|---|---|---|---|
| A. NASA C-MAPSS 真实数据 | 真实、公认基准 | 2.6 万条×4 子集，字段为 21 路传感器+3 工况，**需大量特征工程**；无「磨损量」直接标签，RUL 需自行标定；课程周期内难吃透 | 作为**对照参考** |
| B. 自建指数退化数据 | 退化规律明确、**有磨损量真值**、可控可复现；可定向注入异常值验证清洗流程 | 非真实数据，物理细节简化 | **主用** |
| C. 企业真实数据 | 最贴合 | 课程不要求、无法获取 | 不采用 |

**选择 B 的理由**：本课程重点考核「预测性维护的方法链路是否走通」，而非数据规模。
自建数据能精确控制退化规律与噪声，**便于验证每一步算法是否真的起作用**（例如异常值剔除率可核对）。
同时保留 A 作为公开来源对照，说明方法的可迁移性。

**代价与局限（主动说明）**：仿真数据的噪声模型为高斯分布，比真实工况简单；
后续可用 C-MAPSS 验证泛化性（见「展望」）。

### 2.2 选型二：预测模型 —— RNN vs LSTM vs GRU vs Transformer

| 模型 | 长程依赖 | 参数量/训练成本 | 本任务适配性 | 结论 |
|---|---|---|---|---|
| **RNN** | 弱（梯度易衰减） | **最小** | 窗口仅 **20 步**，序列短，衰减问题不突出 | **选用** |
| LSTM | 强 | 约 4×（三个门） | 能力强但本任务**冗余** | 备选 |
| GRU | 较强 | 约 3× | 折中方案 | 备选 |
| Transformer | 最强 | 最大，且小数据易过拟合 | 20 步短序列上**注意力优势无法体现** | 不采用 |

**选择 RNN 的理由**：
1. **序列长度只有 20**，RNN 的梯度衰减在如此短的序列上不构成瓶颈；
2. **数据量约 1.2 万条**，参数量小的模型更不易过拟合；
3. 课程对应专题明确为「**循环神经网络时序预测（RNN/ReLU）**」，选型与课程知识点对齐；
4. CPU 可训练（实测数十秒完成），符合任务书「无需 GPU」的资源要求。

**代价与局限**：若窗口加长到 100+ 步，RNN 会明显弱于 LSTM/GRU——这是后续改进方向。

### 2.3 选型三：异常值检测 —— 3σ vs 全局 IQR vs 去趋势+IQR

| 方案 | 原理 | 在单调退化序列上的表现 | 结论 |
|---|---|---|---|
| A. 3σ 准则 | 均值±3 倍标准差 | 均值被**趋势主导**，尾部残差大，误判严重 | 不用 |
| B. 全局 IQR | 对原始值求四分位距 | 单调上升序列的**尾部整段被判为异常** | 不用（实测误标约 19%） |
| C. **去趋势 + IQR** | 先扣局部趋势得近似同方差残差，再对残差做 IQR | 残差同方差，正常点误判率降至**方法固有水平（约 1%）** | **选用** |

**关键实现细节**：去趋势用「**两侧邻域中位数**」而非滑动中位数。
因为对单调序列，含当前点的滑动中位数**恒等于当前点**、残差为 0；
取「除当前点外」的两侧邻域中位数（`np.delete(arr, len//2)`）才能既估计趋势、又排除野值干扰。

**实测效果**：剔除异常值 **405 个**（磨损 322 + 温度 281），对约 1.5% 的注入比例识别准确。

### 2.4 选型四：RUL 估算方法 —— 直接回归 vs 滚动前向模拟

| 方案 | 做法 | 优点 | 缺点 | 结论 |
|---|---|---|---|---|
| A. 直接回归 | 训练一个模型直接输出 RUL 值 | 单次前向，快 | 需要 RUL 标签（本项目 RUL 是按分段线性**假设**算的，标签质量依赖假设）；且与磨损预测是**两个独立模型** | 不用 |
| B. **滚动前向模拟** | 复用**同一个**磨损预测模型，反复预测→回填窗口，直到越过失效阈值 | **不额外训练模型**，与磨损预测**同一套权重**、逻辑自洽；本质是状态空间前向搜索 | 需多次前向（20–125 次），但单步仅毫秒级 | **选用** |

**选择 B 的理由**：避免引入第二套模型和不可靠的 RUL 标签，
让「磨损预测」与「寿命估算」共享同一物理依据（都是模型对磨损演化的外推），
同时天然对应课程的「**智能搜索与状态空间搜索**」专题。

### 2.5 选型五：架构与技术栈 —— B/S vs C/S；FastAPI vs Flask；SQLite vs MySQL

| 维度 | 备选 | 选择 | 理由 |
|---|---|---|---|
| 架构 | C/S vs **B/S** | **B/S** | 任务书明确 B/S 或 C/S 均可；B/S **零安装**，浏览器直接演示，答辩现场最稳妥 |
| 后端 | Flask vs **FastAPI** | **FastAPI** | ①自动生成交互式 API 文档（/docs），答辩演示友好；②Pydantic 请求校验，减少手写校验；③原生 async，符合「实时预测」非功能需求 |
| 数据库 | MySQL vs **SQLite** | **SQLite** | 单机演示场景无需独立数据库服务；**零配置、随项目走**，`start.bat` 一键可跑；标准库自带，降低环境依赖 |
| 前端 | React vs **原生 HTML+ECharts** | **原生 HTML+ECharts** | 单页展示需求，引入构建工具链（Node/npm）会增加部署复杂度且与考核点无关；ECharts CDN 即可满足曲线可视化 |
| 可视化 | 仅 Matplotlib vs **Matplotlib+ECharts** | **两者** | Matplotlib 输出静态分析图（训练曲线/预处理对比）供报告用；ECharts 负责前端交互式展示 |

**代价与局限**：SQLite 不适合多用户并发写入，若扩展为车间多终端系统需迁移至 MySQL——属后续工程化方向。

---

## 第三部分　选型的量化验证（用数据支撑，而非口头声称）

| 验证项 | 数据 | 说明 |
|---|---|---|
| 异常值剔除效果 | 剔除 405 个（≈1.5% 注入比例） | 与注入比例吻合，说明检测有效且未过度剔除 |
| 模型预测精度 | 独立测试集 **MAE = 0.0041 mm、RMSE = 0.0051 mm** | 相对失效阈值 1.0 mm，**误差约 0.4%**，满足工程预警需求 |
| 超参搜索有效性 | 4 组配置全部对比，最优 `hidden=32, lr=1e-3`（val_mse≈2.4e-5） | 证明网格搜索在状态空间中确实完成了择优 |
| 泛化性验证 | **按设备划分**训练/测试（24 台 vs 6 台，无重叠） | 测试设备模型从未见过，指标可信，无数据泄漏 |
| 系统可用性 | pytest **14 用例全通过** + uvicorn 冒烟通过 | 证明交付物可运行、可测试 |

---

## 第四部分　答辩问答：选型相关问题应对

| 可能的问题 | 回答要点 |
|---|---|
| 「为什么不用 LSTM？」 | 窗口只有 20 步，RNN 的梯度衰减在此长度不构成瓶颈；数据量 1.2 万条，小模型更不易过拟合；且课程专题就是 RNN/ReLU。窗口加长到 100+ 步时会考虑 LSTM |
| 「自建数据不算真实数据吧？」 | 是仿真数据，但**退化规律（指数退化模型）是 RUL 领域的经典模型**；本项目重点验证方法链路，且保留了 NASA C-MAPSS 作为真实数据对照来源 |
| 「RUL 标签是自己假设的，可靠吗？」 | 正因如此，**没有用假设标签去训第二个模型**，而是用滚动前向模拟从磨损预测模型直接外推 RUL，避免依赖不可靠标签 |
| 「SQLite 算数据库吗？」 | 是完整的关系型数据库（支持 SQL、事务、索引），本场景单机演示零配置更合适；扩展多用户时迁移 MySQL 即可 |
| 「智能搜索体现在哪？」 | 两处：①超参网格搜索在 `hidden_size×lr` 状态空间中以验证集 MSE 择优（实际影响训练）；②RUL 估算用滚动前向状态空间搜索失效时刻 |
| 「仿真数据 0.4% 的误差是不是太容易了？」 | 是，这正是选仿真的代价——噪声为高斯模型，比真实工况简单。所以我在选题论证里主动说明了这一点，并把 C-MAPSS 验证列为展望 |
