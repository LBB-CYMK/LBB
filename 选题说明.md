# 选题说明

## 一、拟定题目

**面向旋转机械磨损型部件的退化趋势预测与预测性维护决策系统**
——基于 RNN 时序建模的磨损量预测、剩余寿命(RUL)估算与三级故障预警

> 参照对象：数控机床**主轴轴承**等旋转机械中的**磨损型退化部件**（同类方法可迁移至导轨、刀具等磨损主导失效的部件）。
> 说明：本项目为课程设计，采用**自建仿真数据**建模，定位是「以该类部件为参照对象的建模与决策链路验证」，非真实产线部署。

## 二、场景具体化（做什么设备、测什么、怎么判失效、谁来用）

| 要素 | 具体化内容 |
|---|---|
| **对象** | 旋转机械中的磨损型退化部件，以**数控机床主轴轴承**为参照对象。失效模式为**渐变型磨损失效**（非突发故障），退化轨迹可拟合、可预测 |
| **采集信号** | 按**运行周期**采集两路信号：①**磨损量 w(t)**（mm，直接退化指标）②**温度 T(t)**（℃，与磨损强相关的间接信号）。二者构成 2 维多特征时序 |
| **失效判据** | 磨损量达到 **1.0 mm** 判定部件失效。数据中 30 台设备寿命落在 **244 ~ 597 周期**（均值 399） |
| **业务闭环** | **运维人员**在系统看板中查看各设备当前磨损与预测趋势 → 系统输出剩余寿命 RUL 与三级预警 → 据此安排检修决策：<br>**正常** → 按计划周期检修；**预警** → 提前准备备件、安排近期停机检查；**严重预警** → 立即安排停机检修 |

**业务价值**：把传统「定期检修 + 固定阈值」升级为「按需检修 + 趋势预测」，
减少检修浪费与非计划停机。

## 三、项目完成目标（可验收指标）

以工业设备运行时序数据为研究对象，完成**数据清洗 → 时序建模 → 磨损预测 → RUL 估算 → 预警输出 → B/S 系统交付**的完整链路：

| 环节 | 具体目标 | 实测结果 |
|---|---|---|
| 数据构建 | 生成 30 台设备退化时序数据，注入约 1.5% 传感器野值 | 12000 条原始样本 |
| 数据预处理 | 完成异常值剔除、归一化、RUL 标签、滑动窗口样本构建 | 剔除异常值 **405 个**（磨损 322、温度 281）；训练集 `(9057,20,2)` / 测试集 `(2343,20,2)` |
| 磨损预测 | RNN 模型预测下一周期磨损量，误差低于失效阈值的 1% | 独立测试集 **MAE = 0.004 mm**（约为失效阈值 1.0 mm 的 **0.4%**），RMSE = 0.0051 mm |
| RUL 估算 | 估算部件剩余寿命，指导检修排期 | 滚动前向模拟输出 RUL（截断上限 125 周期） |
| 故障预警 | 输出正常 / 预警 / 严重预警三级状态 + **可执行检修建议** | 三级预警闭环 |
| 系统交付 | 完整 B/S 架构可运行 demo，通过自动化测试 | 前端 + 后端 API + SQLite + 算法模块；**pytest 14 用例全部通过** |

## 四、涉及技术方向及其在系统中的实际作用

> 任务书要求：**每个技术方向须说明在系统中的实际作用，仅装饰性引入不计入覆盖。**以下逐一对应到代码。

### 1. 数据预处理与异常值识别技术
- **实际作用**：为模型训练提供干净数据，避免野值污染退化趋势拟合。
- **落地位置**：`src/preprocess.py` 的 `iqr_outlier_removal`
- **具体做法**：磨损量呈单调上升趋势，**直接对原始值做 IQR 会把趋势尾部整段误判为异常**（实测误标约 19%）。
  因此先用「**两侧邻域中位数**」去趋势（`np.delete` 排除当前点，避免野值污染趋势估计），得到近似同方差的残差，
  再对残差按 `[Q1−1.5·IQR, Q3+1.5·IQR]` 判定，并用局部趋势值回填。
- **效果**：正确剔除 405 个异常值，与 1.5% 注入比例吻合。

### 2. 循环神经网络时序预测技术（RNN/ReLU）
- **实际作用**：系统核心预测引擎，对退化趋势建模并外推下一周期磨损量。
- **落地位置**：`src/model.py` 的 `WearRNN`、`src/train.py`
- **具体做法**：`nn.RNN(nonlinearity='relu', batch_first=True)` 对连续 20 个周期的 2 维特征编码，
  取**最后时间步隐藏状态**作为整段序列的摘要，经 `Linear(32,16) + ReLU + Linear(16,1)` 回归头输出下一周期磨损量。
- **选型理由**：窗口仅 20 步、数据约 1.2 万条，RNN 的梯度衰减在此长度不构成瓶颈，
  且小参数量模型更不易过拟合；CPU 数十秒即可完成训练。
- **效果**：独立测试集 MAE 0.004 mm。

### 3. 智能搜索与状态空间搜索技术
- **实际作用**：**两处真实参与**，分别作用于**训练调参**与**寿命预测**，非装饰性引入。
- **落地位置一 —— 超参数网格搜索**（`src/train.py`）：
  对 `hidden_size × 学习率` 构成的状态空间枚举 4 组配置，以**验证集 MSE 为评价函数**择优，
  最优配置 `hidden=32, lr=1e-3`（val_mse ≈ 2.42e-5），再以此在完整训练集上重训。**该结果直接决定了最终模型。**
- **落地位置二 —— 滚动前向状态空间模拟**（`src/predict.py` 的 `roll_forward_rul`）：
  从当前窗口出发反复「预测下一周期磨损 → 回填窗口 → 再预测」，在状态空间中**前向搜索磨损首次越过失效阈值的时刻**，
  所得步数即为剩余寿命。**复用同一套模型权重**，无需第二套模型或不可靠的 RUL 标签。

### 4. 版本控制与工程部署技术
- **实际作用**：支撑小步提交、变更可回溯与交付物可复现。
- **落地位置**：Git 全程管理（含完整提交历史）；`prompt/` 保留 AI 会话日志；`start.bat` 一键启动（同目录搜索解释器，不依赖 PATH）。
